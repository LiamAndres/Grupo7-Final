<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Login GPU Online</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include ("../partials/header")%> 
    <!-- fin header -->
    <!-- el header del login solo tiene logo, debes ocultar todo menos el logo -->
    <main>
        <!-- listo para modificar, exitos!! 😄  -->
        <main class="main-login">  <!-- inicio de main-->
            <div class="login">  <!-- inicio class login-->
                <form action="/users/login" method="post">
                    <h3>Ingresa!</h3>   
                    <label for="email"></label>
                    <input type="text" id="email" name="email" placeholder="Escribe tu email">
                    <% if (locals.errors && errors.email) { %>
                        <div class="text-danger">
                            <%= errors.email.msg %> 
                        </div>
                    <% } %>         
                    <label for="password"></label>
                    <input type="password" id="password" name="password" placeholder="Contraseña">
                    <div class="loginRememberCheckbox">
                        <label class="checkbox"> 
                            <h4>Recordarme</h4>
                            <input class="espacio checkbox" type="checkbox" name="remember_user" value="PC Multimedia">
                            <span class="check"></span>
                        </label>
                    </div>
                    <div class="group-buttons-login">
                        <button class="buttons-login" type="submit" >continuar</button>

                        <a class="buttons" href="/users/register">crear cuenta</a>
                    </div> <!-- inicio class group-buttons-->
                    <!-- <input type="button" value="ENVIAR"> -->
                </form>
                
            </div>   <!-- fin class login--> 
        </main>
    </main>
    <footer>
        <!-- sin footer, no contiene footer -->
    </footer>
    
</body>
</html>