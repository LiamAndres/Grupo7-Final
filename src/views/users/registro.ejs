<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Registro GPU Online</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/main.css">    
</head>
<body>
    <!-- hay que dejar solo el logo ATENCION!!! OJOOOO!!!!! CUIDADOOOO!!! ALERTA!!! -->
    <%- include ("../partials/header")%> 
    <!-- fin header -->
    <!-- el header del registro solo tiene logo, debes ocultar todo menos el logo -->
    
        <!-- listo para modificar, exitos!! 😄  -->
        <main>  <!-- inicio de main-->
            <div class="register-google">
                <h3>Crear cuenta</h3>
                <a class="button-google" href=""> <i class="fab fa-google"></i> Entrar con Google</a>
                <h4>----- O bien -----</h4>
            </div>
            <div class="registro"> <!-- inicio class register-->
                <form action="/users/register" method="POST" enctype="multipart/form-data">
                    <h3>Regístrate!</h3>
                    <label class="labelInput" for="nombre">Nombre</label>
                    <input 
                    class="box-shadow <%= locals.errors && errors.first_name ? 'is-invalid' : null %>"
                    value="<%= locals.oldData ? oldData.first_name : null %>" 
                    type="text"  
                    name="first_name" 
                    placeholder="Nombre" 
                    >
                    <% if (locals.errors && errors.first_name) { %>
                        <div class="text-danger">
                            <%= errors.first_name.msg %> 
                        </div>
                    <% } %>

                    <label class="labelInput" for="apellido">Apellido</label>
                    <input 
                    class="box-shadow <%= locals.errors && errors.last_name ? 'is-invalid' : null %>"
                    value="<%= locals.oldData ? oldData.last_name : null %>" 
                    type="text"  
                    name="last_name" 
                    placeholder="Apellido" 
                    >
                    <% if (locals.errors && errors.last_name) { %>
                        <div class="text-danger">
                            <%= errors.last_name.msg %> 
                        </div>
                    <% } %>

                    <label class="labelInput" for="nombreusuario">nombre de usuario</label>
                    <input 
                    class="box-shadow <%= locals.errors && errors.user_name ? 'is-invalid' : null %>"
                    value="<%= locals.oldData ? oldData.user_name : null %>" 
                    type="text"  
                    name="user_name" 
                    placeholder="Ej: user123" 
                    >
                    <% if (locals.errors && errors.user_name) { %>
                        <div class="text-danger">
                            <%= errors.user_name.msg %> 
                        </div>
                    <% } %>

                    <label class="labelInput" for="email">Email</label>        
                    <input 
                    class="box-shadow <%= locals.errors && errors.email ? 'is-invalid' : null %>"
                    value="<%= locals.oldData ? oldData.email : null %>" 
                    type="email"  
                    name="email" 
                    placeholder="E-mail" 
                     >
                    <% if (locals.errors && errors.email) { %>
                        <div class="text-danger">
                            <%= errors.email.msg %> 
                        </div>
                    <% } %>

                    <label class="labelInput" for="password">Contraseña</label>
                    <input 
                    class="box-shadow <%= locals.errors && errors.password ? 'is-invalid' : null %>" 
                    type="password"  
                    name="password" 
                    placeholder="Elegí tu contraseña"
                    >
                    <% if (locals.errors && errors.password) { %>
                        <div class="text-danger">
                            <%= errors.password.msg %> 
                        </div>
                    <% } %>

                    <!--  ATENCIOOON OJOOOOOO CUIDADOOOO no borrar seegundo password verificacion -->
                    <!-- <label for="password"></label>
                    <input class="box-shadow" type="password"  name="password2" placeholder="Repite tu contraseña"required> -->
                    
                    <h4>Fecha de nacimiento</h4>
                    <input class="box-shadow" type="date" name="mes" id="mes" >
                    <input class="box-shadow" type="text" name="" id="date" placeholder="Domicilio">
                    
                    <h4>Intereses</h4>
                    
                        <label for="hobby"> <!-- inicio hobby-->

                            <div class="hobby-users"> <!-- inicio hobby-users-->
                                <label class="checkbox"> PC Multimedia
                                    <input class="espacio checkbox" type="checkbox" name="hobby" value="PC Multimedia">
                                    <span class="check"></span>
                                </label>
                                <label class="labelTitle checkbox">
                                    <input class="espacio checkbox" type="checkbox" name="hobby" value="PC Gaming">PC Gaming
                                    <span class="check"></span>
                                </label>
                                <label class="labelTitle checkbox">
                                    <input class="espacio checkbox" type="checkbox" name="hobby" value="PC Edición">PC Edición
                                    <span class="check"></span>
                                </label>
                                <label class="labelTitle checkbox">
                                    <input class="espacio checkbox" type="checkbox" name="hobby" value="PC Streaming">PC Streaming
                                    <span class="check"></span>
                                </label>
                            </div>   <!-- fin hobby-users-->
                        </label> <!-- fin hobby-->
                        
                    <!-- inicio enviar archivo foto-->
                    <h4>Elige foto de perfil</h4>
                    
                    <input 
									type="file" 
									name="avatar"
									class="formGroup" <%= locals.errors && errors.avatar ? 'is-invalid' : null %>
								>
								<% if (locals.errors && errors.avatar) { %>
									<div class="text-danger">
										<%= errors.avatar.msg %>
									</div>
								<% } %>
                    <!-- fin enviar archivo foto-->    
                    
                    <div class="group-buttons group-buttons-register">
                        <!-- <button class="buttons buttons-register" type="reset" >borrar</button> -->
                        <button class="buttons buttons-register" type="submit">Crear cuenta</button>
                    </div>   <!-- inicio class group-buttons-->
                    <!-- <input type="button" value="ENVIAR"> -->
            
                    
                </form>
            </div> <!-- fin class registro-->
        </main>
    
    <footer>
        <!-- sin footer, no contiene footer -->
    </footer>
    
</body>
</html>